# macOS
.DS_Store
.AppleDouble

# Python
__pycache__/
*.pyc
*.pyo
.Python
*.egg-info/

# Secrets / local env (never commit API keys)
.env
.env.local
.env.*.local

# Vercel local project link
.vercel/

# Large / private files
audit.jsonl

# RAG build temp artifacts (regenerable; the committed outputs are
# data/rag/embeddings.npz + data/rag/chunks_meta.json)
data/rag/_emb_cache.jsonl
data/rag/_embed_run.log

# Old/generated root-level output (keep dist/ version)
/conan-fan-map.html
/favicon-preview.html
# Old dist filenames (replaced by dist/index.html)
/dist/conan-fan-map.html
/dist/favicon-preview.html

# Legacy folders
outputs/
shim-lib/
shim-perm/
uploads/

# Editor / IDE
.vscode/
.idea/
*.swp
.gstack/
